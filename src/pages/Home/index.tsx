import {ExampleTable} from '@sections';
import React from 'react';

import styles from './index.module.scss';

export const Home: React.FC = () => {
    return (
        <div>
            <ExampleTable />
        </div>
    );
};
